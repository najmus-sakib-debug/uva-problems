#include<stdio.h>
int main()
{
    char a[30];
    double b,c,d;
    gets(a);
    scanf("%lf%lf", &b, &c);
    d = b + (c * (15.0/100));
    printf("TOTAL = R$ %.2lf", d);
    return 0;
    puts(a);
    return 0;
}
